namespace GameFlow.Utils
{
    /// <summary>
    /// Represents a unit type that carries no information.
    /// This is used as a placeholder type for ports that don't need to pass or return data.
    /// Similar to void, but can be used in generic type parameters.
    /// </summary>
    public readonly struct Unit
    {
    }
}